# Runtime dependencies: only packages actually imported along the
# src/agent.py -> src/graph/*.py -> src/clients/*.py -> prompts/loader.py path. This file
# is the pip_requirements ops/deploy_model.py packages for Model Serving — don't add
# packages here that are only needed by the provisioning scripts (ops/) or tests; those
# go in the root requirements.txt instead.
databricks-sdk>=0.40.0
databricks-langchain>=0.3.0
langgraph>=0.2.0
langchain-core>=0.3.0
mlflow>=2.18.0
python-dotenv>=1.0.1
pydantic>=2.7.0
pyyaml>=6.0  # used by prompts/loader.py to parse .prompt file frontmatter
