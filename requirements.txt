# Full dependency set for local development / provisioning / testing. Only
# requirements-runtime.txt is actually deployed to Model Serving (see deploy_model.py's
# _REQUIREMENTS_FILE).
-r requirements-runtime.txt

# Used only by the ops/ provisioning scripts; the runtime code never imports these
databricks-connect>=15.4.0
azure-core>=1.30.0
azure-storage-file-datalake>=12.14.0
python-docx>=1.1.0

# Tests
pytest>=8.0.0
